import random
import math
from Distance.euclidean import *

"""Takes in input data, number of ants, 
and number of iterations.  blah blah blah.
Returns formed clusters."""

def aco(inputs, ants, iterations):
    clusters = []
    return clusters
